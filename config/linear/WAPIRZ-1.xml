<?xml version="1.0" encoding="utf-8"?>
G<Product xmlns='http://code.google.com/p/open-zwave/'>
	<!-- Configuration Parameters -->
	<CommandClass id="112">
		<Value type="byte" index="1" genre="config" label="Re-trigger Wait Time" size="2" min="0" max="255" value="3">
			<Help>By default, to conserve battery life, after the WAPIRZ-1 detects motion it cannot
			be re-triggered by motion again for 3 minutes. Each time motion is detected
			(even during this wait time) the 3 minute timer starts over. The length of the
			re-trigger wait time can be adjusted with this setting.
			</Help>
		</Value>
	</CommandClass>
</Product>

